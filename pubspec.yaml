name: flutter_value_localization
description: >
  Flutter 本地化适配层，支持从 Assets 或内存 Map 加载多语言数据。
  Flutter localization adapter supporting both Assets and in-memory Map data sources.
version: 0.0.2
homepage: https://github.com/ValueNotify666/value_localization_core.git
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  value_localization_core:
    git:
      url: https://github.com/ValueNotify666/value_localization_core.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
