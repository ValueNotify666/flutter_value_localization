name: flutter_value_localization
description: >
  Flutter 本地化适配层，支持从 Assets 或内存 Map 加载多语言数据。
  Flutter localization adapter supporting both Assets and in-memory Map data sources.
version: 0.0.3
homepage: https://github.com/ValueNotify666/flutter_value_localization
repository: https://github.com/ValueNotify666/flutter_value_localization.git
issue_tracker: https://github.com/ValueNotify666/flutter_value_localization/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  value_localization_core: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
