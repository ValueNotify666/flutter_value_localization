name: flutter_value_localization
description: Flutter adapter layer for value_localization_core.
publish_to: none
version: 0.0.1
homepage:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  value_localization_core:
    git:
      url: https://github.com/ValueNotify666/value_localization_core.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
